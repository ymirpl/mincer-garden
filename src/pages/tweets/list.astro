---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ListIndex from '../../components/ListIndex.astro';
import { getItemsByType } from '../../lib/content';

const items = await getItemsByType('tweet');
---

<BaseLayout title="tweets list" view="list">
  <ListIndex items={items} heading="tweets" />
</BaseLayout>
