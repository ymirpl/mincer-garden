---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ListIndex from '../../components/ListIndex.astro';
import { getItemsByType } from '../../lib/content';

const items = await getItemsByType('product');
---

<BaseLayout title="products" view="list">
  <ListIndex items={items} heading="products" />
</BaseLayout>
