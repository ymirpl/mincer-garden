---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ListIndex from '../../components/ListIndex.astro';
import { getItemsByType } from '../../lib/content';

const items = await getItemsByType('link');
---

<BaseLayout title="links" view="list">
  <ListIndex items={items} heading="links" />
</BaseLayout>
