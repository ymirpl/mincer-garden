---
interface Props {
  view?: 'mosaic' | 'list' | 'note';
}

const { view = 'mosaic' } = Astro.props;
---

<header class="site">
  <a class="wordmark" href="/" aria-label="garden.mincer home">
    <span class="sprig" aria-hidden="true">
      <svg class="leaf" width="14" height="14" viewBox="0 0 14 14" fill="none">
        <path d="M2 12 C 4 6, 8 3, 12 2 C 12 7, 9 11, 4 12 Z" />
        <path d="M2.5 11.5 L 9 5" />
      </svg>
    </span>
    garden.mincer
  </a>
  <nav class="views" aria-label="primary">
    <a href="/" class:list={{ active: view === 'mosaic' }} aria-current={view === 'mosaic' ? 'page' : undefined}>mosaic</a>
    <span class="sep" aria-hidden="true">·</span>
    <a href="/list" class:list={{ active: view === 'list' }} aria-current={view === 'list' ? 'page' : undefined}>notes</a>
  </nav>
</header>
